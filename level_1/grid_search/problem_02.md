# グリッド探索 問題2：8方向移動

## 問題

H行W列のグリッドが与えられる。`.`は通路、`#`は壁である。
スタート `S` からゴール `G` まで、上下左右斜めの8方向に移動できるとき、最短移動回数を求めよ。
到達不可能な場合は `-1` を出力せよ。

## 入力

```
H W
grid (H行)
```

## 出力

最短移動回数（到達不可能なら -1）

## 入力例

```
3 5
S....
.###.
....G
```

## 出力例

```
3
```
